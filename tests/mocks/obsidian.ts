// Runtime stub for the `obsidian` package, which ships type declarations only.
// vitest.config.mjs aliases `obsidian` here so modules with value imports
// (e.g. `instanceof TFile` checks) can load under vitest.
export class TFile {}
